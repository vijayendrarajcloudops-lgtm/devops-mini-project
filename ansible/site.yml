---
- name: Configure EC2 using Ansible
  hosts: aws
  become: true

  tasks:
    - name: Update apt cache
      apt:
        update_cache: true

    - name: Install NGINX
      apt:
        name: nginx
        state: present

    - name: Start and enable NGINX
      service:
        name: nginx
        state: started
        enabled: true

